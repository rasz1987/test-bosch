import { makeStyles } from '@material-ui/styles';

export default makeStyles({
  content: {
    display: 'flex',
    justifyContent: 'center',
    flexDirection: 'row'
  },
  item: {
    marginTop: '25px'
  }
});